# Copie este arquivo para `.env.local` e preencha os valores.
# `.env.local` está no .gitignore e não será commitado.

# Chave usada apenas no servidor (Serverless Functions)
GOOGLE_API_KEY=coloque_sua_chave_aqui

